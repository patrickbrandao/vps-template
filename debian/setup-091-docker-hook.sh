#!/bin/bash

# Hook de firewall no Docker
#========================================================================

    # script de hook
    # HS="/etc/local-up.d/docker-firewall-hook";
    # HU="https://tmsoft.com.br/temp/docker-firewall-hook.sh";
    # [ -f "$HS" ] || { wget -O "$HS" "$HU"; chmod +x "$HS"; };
    # sh $HS;

    # # Pasta com scripts de hook:
    # mkdir -p /etc/docker/firewall;


exit 0;
